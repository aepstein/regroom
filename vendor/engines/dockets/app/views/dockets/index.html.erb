<% content_for :body_content_left do %>
  <ul id="dockets">
    <% @dockets.each do |docket| %>
      <li>
        <%= link_to docket.name, docket_url(docket) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
